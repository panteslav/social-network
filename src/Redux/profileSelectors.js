export const getPosts = (state) => state.profilePage.posts;

export const getProfileData = (state) => state.profilePage.profileData;

export const getProfileStatus = (state) => state.profilePage.profileStatus;

export const getFollowStatus = (state) => state.profilePage.followStatus;
